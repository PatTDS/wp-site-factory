{
  "name": "{{PROJECT_NAME}}-theme",
  "version": "1.0.0",
  "description": "WordPress theme for {{COMPANY_NAME}}",
  "scripts": {
    "dev": "tailwindcss -i ./assets/css/input.css -o ./assets/css/main.css --watch",
    "build": "tailwindcss -i ./assets/css/input.css -o ./assets/css/main.css --minify",
    "watch": "npm run dev"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.10"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
